Configuration files documentation
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

The configuration of the AIME API Server and its endpoints is set in configuration files using the toml standard. Settings concerning all endpoints and the server itself are set in the server configuration file. 
To adjust settings concerning specific endpoints there is one endpoint configuration file for each endpoint.

.. toctree::
   :maxdepth: 1

   Server <server_config>
   Endpoint <endpoint_config>
